apiVersion: apps/v1
kind: Deployment
metadata:
  name: easy-kart-products
  namespace: easy-kart
spec:
  replicas: 1
  selector:
    matchLabels:
      app: easy-kart-products #This should be same label
  template:
    metadata:
      labels:
        app: easy-kart-products #This should be same label
    spec:
      containers:
      - name: easy-kart-products
        image: pdesaidocker/easy-kart-api-products:{{imageTag}}
        resources:
          requests:
            memory: "256Mi"   # Minimum memory for the container to function
            cpu: "250m"       # Minimum CPU for the container to function
          limits:
            memory: "512Mi"   # Maximum memory that can be used
            cpu: "500m"       # Maximum CPU that can be used
        ports:
        - containerPort: 8080 #port defined in image dockerfile
        env:
        - name: CosmosDB__endpoint
          valueFrom:
            secretKeyRef:
              name: easy-kart-secret  
              key: cosmosendpoint
        - name: CosmosDB__authKey
          valueFrom:
            secretKeyRef:
              name: easy-kart-secret  
              key: cosmosauthkey
